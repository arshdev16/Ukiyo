export type cardData = {
  name: string;
  imgUrl: string;
  price: number;
  slug: string;
};

export type CartItemData = {
  id: number;
  name: string;
  price: number;
  quantity: number;
  total: number;
  productImage: string;
}
